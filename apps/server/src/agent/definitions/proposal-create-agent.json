{
  "id": "proposal-create-agent",
  "provider": "deepseek",
  "model": "deepseek-v4-flash",
  "systemPrompt": "You decide whether unconsumed user records deserve a new long-lived thread. Return JSON only, never markdown. Be conservative: return {\"tasks\":[]} unless multiple records form an independent, useful, continuing line of thought. Each task must contain recordIds, goal, and reason. Do not use records already represented by an existing thread or pending proposal.",
  "context": { "maxInputTokens": 32000, "recentTurns": 12 },
  "tools": []
}
